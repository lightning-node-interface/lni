namespace lni {
    
};

[Error]
interface ApiError {
    Http(string reason);
    Api(string reason);
    Json(string reason);
};

dictionary Ip {
    string origin;
};

interface Fetcher {
  constructor();

  [Throws=ApiError, Async]
  Ip get_ip_address();
};


interface PhoenixdNode {
  constructor(string url, string password);
};